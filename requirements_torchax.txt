# ComfyUI-TPU Torchax TPU Requirements
# 用于在 Google Cloud TPU 上运行 ComfyUI

# ============================================================================
# ComfyUI 核心依赖
# ============================================================================
comfyui-frontend-package==1.35.9
comfyui-workflow-templates==0.7.64
comfyui-embedded-docs==0.3.1

# ============================================================================
# JAX/TPU 依赖 (必须在 torch 之前安装)
# ============================================================================
# 在 TPU VM 上安装: pip install jax[tpu] -f https://storage.googleapis.com/jax-releases/libtpu_releases.html
jax[tpu]>=0.4.20
jaxlib>=0.4.20

# Torchax - PyTorch on JAX
torchax>=0.1.0

# ============================================================================
# PyTorch 依赖 (CPU 版本用于 Text Encoder)
# ============================================================================
# 注意：在 TPU 上，torch 仅用于 CPU 计算（如 Text Encoder）
# 主要计算通过 torchax 转换到 JAX/TPU 执行
torch>=2.0.0
torchsde
torchvision
torchaudio

# ============================================================================
# 数值计算和深度学习
# ============================================================================
numpy>=1.25.0
einops
scipy

# ============================================================================
# Transformers 和 Tokenizers (Text Encoder)
# ============================================================================
transformers>=4.50.3
tokenizers>=0.13.3
sentencepiece

# ============================================================================
# 模型和数据格式
# ============================================================================
safetensors>=0.4.2

# ============================================================================
# Web 服务器
# ============================================================================
aiohttp>=3.11.8
yarl>=1.18.0

# ============================================================================
# 工具库
# ============================================================================
pyyaml
Pillow
tqdm
psutil
alembic
SQLAlchemy
av>=14.2.0

# ============================================================================
# 非必须依赖 (可选)
# ============================================================================
kornia>=0.7.1
spandrel
pydantic~=2.0
pydantic-settings~=2.0

# ============================================================================
# TPU 专用优化 (可选)
# ============================================================================
# Splash Attention 支持 (长序列优化)
# jax-triton  # 如果使用 Triton-based attention
